# Optional: full API origin for production builds (default: https://heavenhub-production.up.railway.app)
# VITE_API_BASE_URL=https://heavenhub-production.up.railway.app

# Optional: where `npm run dev` proxies /api (default: Railway URL above). Use for local Spring:
# VITE_DEV_PROXY_TARGET=http://localhost:8080
